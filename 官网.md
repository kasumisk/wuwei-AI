你是 Flutter + NestJS + Firebase Cloud Messaging 的生产级推送系统架构师。

请基于当前 EatCheck 项目实现一套生产级 Push Notification MVP。

项目技术栈：
- Flutter App (在这里目录 /Users/xiehaiji/project/kasumi/flutter/flutter-scaffold)
- NestJS Backend
- Prisma
- PostgreSQL
- Firebase Auth
- Firebase Cloud Messaging
- Redis (生产环境 Cloud Jobs)

目标：
实现 EatCheck 的留存型推送系统，不做营销轰炸，优先支持每日提醒、周报提醒、分析结果 follow-up 和通知点击跳转。

请先完整阅读当前项目结构和代码，不要破坏现有功能。

实现范围：

1. Flutter 端
- 集成 Firebase Messaging
- 请求通知权限
- 获取 FCM Token
- 登录后上传 Token 到后端
- Token refresh 时自动更新
- 退出登录时解绑 Token
- 支持前台通知展示
- 支持后台/冷启动点击通知跳转
- 支持 deep link payload：
  - home
  - analysis_detail
  - weekly_report
  - premium

2. NestJS 后端
新增 push 模块：
- PushModule
- PushService
- PushController
- PushScheduler
- PushTemplateService

实现 API：
- POST /api/push/register-token
- POST /api/push/unregister-token
- GET /api/push/preferences
- PATCH /api/push/preferences
- POST /api/push/test

3. Prisma 数据模型
新增：
- PushDeviceToken
- PushNotificationLog
- UserNotificationPreference

要求：
- 一个用户可有多个设备
- token 必须可失效
- 保存 platform、timezone、locale、appVersion、lastSeenAt
- 推送发送必须写日志
- 发送失败要记录 error
- 无效 token 要自动禁用

4. 推送类型
第一版支持：
- DAILY_CHECK_IN
- NO_ANALYSIS_TODAY
- WEEKLY_REPORT_READY
- ANALYSIS_FOLLOW_UP
- PREMIUM_UPGRADE_HINT

5. 定时任务
实现：
- 每日提醒
- 当天未分析提醒
- 每周报告提醒

要求：
- 必须按用户 timezone 发送
- 不要在用户夜间发送
- 支持用户关闭某类推送
- 避免重复发送
- 支持后续扩展到 Redis queue / BullMQ

6. 推送内容设计
请实现 templates：
- 英文优先
- 预留 i18n
- 文案要符合 EatCheck 品牌：
  "Smarter food choices, every day."

7. 安全与合规
- 不能给未授权用户推送
- 用户关闭后不能继续发
- 支持删除/失效 token
- 不在推送里展示敏感健康信息
- 推送内容避免医疗建议

8. 输出要求
请直接给出：
- 数据库 schema 修改
- NestJS 模块代码
- Controller / Service / Scheduler 代码
- Flutter 集成代码
- Firebase 配置说明
- iOS 配置说明
- Android 配置说明
- 测试流程
- 常见坑检查清单

要求：
生产级、可扩展、符合现有项目架构，不要只给概念。


新增要求：

推送系统必须从架构层支持“海外版”和“中国大陆版”双推送体系。

不要将 Firebase Cloud Messaging 写死为唯一 Provider。

必须实现 Push Provider Abstraction Layer。

目标：

业务层不感知具体推送平台。

━━━━━━━━━━━━━━━━━━━
# 推送 Provider 抽象要求
━━━━━━━━━━━━━━━━━━━

请设计：

PushProvider Interface

支持未来扩展：

- Firebase Cloud Messaging（海外）
- JPush / 极光推送（中国大陆）
- 华为 Push Kit（可选）
- Mock Provider（开发环境）

必须：

- Provider 可插拔
- 可按 region/providerType 自动切换
- 不污染业务层
- 支持 batch send
- 支持 token validation
- 支持 provider fallback

━━━━━━━━━━━━━━━━━━━
# 区域化要求
━━━━━━━━━━━━━━━━━━━

必须支持：

- GLOBAL
- CHINA_MAINLAND

未来可扩展：

- EU
- JAPAN
- KOREA

用户/device/token 必须保存：

- pushRegion
- providerType
- locale
- timezone
- deviceBrand
- romType

━━━━━━━━━━━━━━━━━━━
# Flutter 要求
━━━━━━━━━━━━━━━━━━━

Flutter 端不要直接散落 FirebaseMessaging 调用。

必须：

- 抽象 PushManager
- PushAdapter Interface
- FcmAdapter
- ChinaPushAdapter（预留）

支持未来：

- app flavor
- region switching
- 国内/海外双包

━━━━━━━━━━━━━━━━━━━
# 后端要求
━━━━━━━━━━━━━━━━━━━

NestJS PushModule 必须支持：

- ProviderFactory
- ProviderRegistry
- Dynamic Provider Resolution

业务层只能调用：

PushService.send()

不能直接调用 FCM SDK。

━━━━━━━━━━━━━━━━━━━
# 输出要求
━━━━━━━━━━━━━━━━━━━

请输出：

- Provider 抽象架构
- Interface 设计
- Prisma schema 更新
- NestJS ProviderFactory 实现
- Flutter PushManager 实现
- region/provider 选择策略
- 双包/flavor 推荐方案
- 中国大陆推送兼容性建议
- 未来扩展建议

要求：

生产级
全球化架构
长期可维护
适合 EatCheck 后续全球化扩张



完成nestjs服务和flutter App