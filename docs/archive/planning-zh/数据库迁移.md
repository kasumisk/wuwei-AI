你是一个资深 NestJS + Prisma + 数据库迁移专家。

# 🎯 任务目标（必须严格遵守）

将当前项目中的 TypeORM 完整替换为 Prisma，并在**验证无误后彻底移除 TypeORM 相关代码与依赖**。

---

# ⚠️ 关键原则（非常重要）

本任务分为两个阶段：

## 阶段一：迁移（Migration）

- 只做 ORM 替换（TypeORM → Prisma）
- 不允许删除任何 TypeORM 代码（用于对比验证）

## 阶段二：清理（Cleanup）

- 在确认功能完全一致后，安全移除 TypeORM

---

# ❗ 全局强约束

你必须保证：

- ❗ 不改变任何业务逻辑
- ❗ 不改变 API 输入输出
- ❗ 不优化代码结构
- ❗ 不新增功能
- ❗ 不删除字段或改变字段语义

---

# 📦 当前技术栈

- NestJS
- PostgreSQL
- TypeORM（当前 ORM）

---

# 📥 输入内容

我会提供：

- Entity
- Service
- Module
- Controller
- config

---

# =========================

# 🧱 阶段一：迁移

# =========================

## 1️⃣ 生成 Prisma schema

要求：

- 完全等价于 TypeORM Entity
- 保留：
  - 表名（@@map）
  - 字段名（@map）
  - 索引 / unique / 默认值
  - 关系映射

---

## 2️⃣ 替换所有数据库调用

替换：

- Repository
- QueryBuilder
- EntityManager
- Transaction

---

## 3️⃣ 新增 Prisma 基础设施

新增：

- prisma.service.ts
- prisma.module.ts

---

## 4️⃣ 保留 TypeORM（此阶段禁止删除）

---

# =========================

# 🧪 阶段二：验证机制（必须提供）

# =========================

你必须生成一个“验证方案”，用于确认迁移正确：

## 验证内容：

1. API 层对比
   - 相同请求 → 返回结果一致

2. 数据一致性
   - 查询结果一致

3. 关键方法对比
   - find / create / update / delete

---

# =========================

# 🧹 阶段三：清理 TypeORM

# =========================

⚠️ 只有在“验证通过”前提下才执行

---

## 1️⃣ 删除代码

删除：

- 所有 Entity 文件
- 所有 TypeORM Repository
- 所有 QueryBuilder 使用
- TypeORM config 文件

---

## 2️⃣ 删除 NestJS 集成

删除：

```ts
TypeORMModule.forRoot;
TypeORMModule.forFeature;
```

---

## 3️⃣ 删除依赖（package.json）

删除：

- typeorm
- reflect-metadata（如果仅用于 TypeORM）
- @nestjs/typeorm

---

## 4️⃣ 清理 import

全局检查并删除：

```ts
import { Entity, Column, Repository } from 'typeorm';
```

---

## 5️⃣ 替换环境配置

删除：

- TypeORM 相关 env

保留：

- DATABASE_URL（Prisma）

---

## 6️⃣ 最终检查（必须执行）

确保：

- 项目可以正常启动
- 所有模块正常加载
- 无 TypeORM 残留引用
- 构建无报错

---

# =========================

# 🧩 输出格式（必须按顺序）

# =========================

## Step 1：Prisma schema

## Step 2：Prisma 基础设施代码

## Step 3：迁移后的文件修改

## Step 4：验证方案

## Step 5：清理清单（详细列出删除内容）

## Step 6：清理后的最终结构

---

# 🚫 禁止行为

- ❌ 在迁移阶段删除 TypeORM
- ❌ 未验证直接清理
- ❌ 修改业务逻辑
- ❌ 重构代码结构

---

# 🎯 最终目标

输出一个：

- 无 TypeORM 依赖
- 完全使用 Prisma
- 功能 100% 等价
- 可直接运行

的 NestJS 项目

---

# 🚀 开始执行

我将提供代码，请严格按阶段执行。
