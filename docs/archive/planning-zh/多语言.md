你现在是一个 NestJS 架构师 + 国际化专家 + 大型系统重构专家。

⚠️ 本任务是“全量重构任务”，必须一次性完整输出，不能遗漏任何文案或模块。

========================

# 🎯 项目目标（必须实现）

我要对现有 NestJS 项目进行 i18n 重构，实现：

1. 替换所有硬编码文案（不能漏）
2. 删除旧的多语言实现（如散乱的翻译方式）
3. 建立“模块级 i18n 目录结构”
4. 每个模块独立管理自己的多语言
5. 每种语言一个文件（按模块拆分）

========================

# 🧩 目标目录结构（必须遵守）

```id="t42t6c"
src/
  modules/
    user/
      user.module.ts
      user.service.ts
      i18n/
        en.json
        zh.json
        zh_TW.json
        ...

    analysis/
      analysis.module.ts
      i18n/
        en.json
        zh.json
        ...
    diet/
      diet.module.ts
      i18n/
        en.json
        zh.json
        ...
```

要求：

- 每个模块必须有 i18n 目录
- 每个语言一个文件
- 文件中包含该模块全部文案

========================

# 🧩 Step 1：文案扫描（必须完整）

扫描所有模块代码中的：

- throw new Error("...")
- HttpException("...")
- Validation message
- service 返回 message
- AI分析结果文案
- log中误用的用户提示
- DTO validation message（class-validator）

❌ 忽略：

- debug log
- 内部变量

========================

# 🧩 Step 2：Key 设计（模块内 namespace）

每个模块内部使用局部 key：

例如 user 模块：

```id="9t48g3"
{
  "loginFailed": "Invalid email or password",
  "userNotFound": "User not found"
}
```

调用方式：

user.loginFailed
analysis.proteinLow

规则：

1. key 使用 camelCase
2. 不要重复 module 前缀（模块已隔离）
3. 必须语义清晰

========================

# 🧩 Step 3：i18n 实现方案（必须完整）

基于 NestJS：

推荐实现：

- 支持动态加载模块语言文件
- 支持 language fallback（en）

必须实现：

1. 全局 Language Middleware：
   - 从 header 获取：
     - Accept-Language
     - x-lang

2. request 上挂载 language

3. 提供统一调用方式：

```id="ff4ud6"
i18n.t('user.loginFailed')
```

========================

# 🧩 Step 4：代码替换（必须完整）

例如：

❌ 替换前：

```ts
throw new BadRequestException('User not found');
```

✅ 替换后：

```ts
throw new BadRequestException(i18n.t('user.userNotFound'));
```

必须输出：

👉 替换前代码
👉 替换后代码

========================

# 🧩 Step 5：生成语言文件（核心）

必须输出：

每个模块的：

```json
// user/i18n/en.json
{
  "loginFailed": "...",
  "userNotFound": "..."
}
```

要求：

1. 所有 key 必须完整
2. 不允许遗漏
3. 文案必须产品级（自然）
4. AI相关文案必须包含

========================

# 🧩 Step 6：AI系统适配（关键）

必须设计：

analysis 模块：

```id="eoj1c3"
{
  "proteinLow": "...",
  "fatHigh": "...",
  "carbOptimal": "..."
}
```

recommendation 模块：

```id="m3k77d"
{
  "increaseProtein": "...",
  "reduceSugar": "..."
}
```

用于：

AI返回结构 → key → 多语言

========================

# 🧩 Step 7：完整性校验（必须）

输出报告：

1. 总扫描文案数量：X
2. 替换数量：X
3. 未替换：0（必须）
4. 模块数量：X
5. 每个模块是否有 i18n：是
6. key 是否冲突：否

========================

# 🚨 强约束（必须遵守）

1. 不允许遗漏任何文案
2. 不允许只做部分模块
3. 不允许使用示例代替真实结构
4. 不允许省略代码
5. 如果过长：
   👉 自动分 Part 输出
6. 最后一部分必须包含所有模块语言文件

========================

# 🔥 高级要求（必须做）

1. 自动去重文案（跨模块可复用 common）
2. 提供 common 模块（如 error/common）

```id="1kue7t"
common/i18n/en.json
```

3. 支持 fallback：

缺少 zh → 自动 fallback en

========================

# 🧩 Step 8：输出内容（必须完整）

必须输出：

1. 重构后的目录结构
2. 所有模块 i18n 文件（en）
3. 替换后的代码
4. i18n service 实现
5. middleware 实现
6. 校验报告

========================

在最终输出前，请执行：

1. 二次扫描所有模块代码
2. 确认每个模块都有 i18n 目录
3. 对比：
   - 原始文案数量
   - i18n key 数量

如果不一致，必须补全

# ✅ 完成标志

最后必须输出：

"✅ ALL NESTJS I18N MODULES GENERATED SUCCESSFULLY"

```

```
