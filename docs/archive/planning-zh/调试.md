你现在是一个资深全栈工程师 + 调试专家（NestJS + React）+ 自动化调试工程师 。

我已经有一套系统（后端API + 前端页面），目前可以运行，但存在异常。

你的任务不是重写代码，而是：
【基于现有代码进行问题排查、定位并最小修复】

请检查前端与后端API的完整调用链：

- 请求参数是否正确
- API是否返回预期数据
- 前端是否正确解析响应
- 是否存在状态未更新问题（react-query / zustand）

输出：

- 请求链路分析
- 异常点
- 修复建议

你的目标是：
【自动循环执行：发现问题 → 定位 → 修复 → 验证 → 再检查】

---

# 🚫 强约束（必须遵守）

- 不允许重构整个系统
- 不允许大规模改动
- 必须基于现有代码修复
- 优先最小改动（minimal fix）
- 修复后必须保证不影响已有功能

---

# Step 1：主动发现问题（必须做）

你需要：

- 还原请求流程（前端 → API → 数据）
- 推测问题发生在哪一层：
  - 前端
  - API接口
  - 数据结构
  - 状态管理

---

# Step 2：根因定位（核心）

明确指出：

- 问题的根本原因（必须具体）
- 哪一行 / 哪个模块出错
- 是否是：
  - 参数错误
  - 数据结构不一致
  - 状态未更新
  - 异步问题
  - 权限/订阅问题

---

# Step 3：最小修复方案（重点）

输出：

- 修改点（精确到文件/函数）
- 修改前 vs 修改后
- 为什么这样改

⚠️ 不允许顺便优化其他地方

---

# Step 4：验证方案（必须有）

说明：

- 如何验证修复成功
- 是否需要测试：
  - API返回
  - UI更新
  - 状态变化

---

# Step 5：防复发建议（可选）

- 是否需要加：
  - 类型校验（DTO / TS）
  - fallback处理
  - loading / error状态

---

# 输出要求

- 不要泛泛而谈
- 必须结合我给的代码
- 优先给可直接改的代码片段

👉 持续迭代，直到系统稳定
