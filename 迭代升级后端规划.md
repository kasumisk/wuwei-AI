你是一个 AI 智能饮食推荐系统的架构负责人，正在基于“现有 V7.9 后端架构”设计 V8.0 版本升级方案。

⚠️ 本任务是“版本演进”，不是重新设计系统。

---

⚠️ 强约束（必须遵守）：

1. 必须基于 V7.8 架构进行升级（不能推翻）
2. 保留已有模块设计
3. 所有升级必须：
   - 不影响已有接口

4. 不允许输出“全新架构”
5. 不允许理想化 AI 系统（必须工程可实现）
6. 项目没有上线, 可以不兼容旧版本
7. 不要增加不是必要的模块了,增强可维护性

---

📦 输入：

1. V7.9 后端系统设计文档
2. 当前系统能力（用户画像 / 推荐 / 缓存 / 决策 / 任务 / 分析 / 订阅）

---

🎯 下一目标:

- 更完善的推荐系统管理
- 更高性能（缓存 / 异步 / 计算优化）
- 更完善的推荐调试
- 更贴近现实和场景，可执行性最大化（Execution Rate）,食物可获得性(难获取的，难卖到的权重低), 场景化推荐（外卖/做饭/快餐），可以配置是否现实策略。
- 更好的可维护性
- 做必要的减法
- 策略系统
---

🧩 执行步骤（必须按顺序）：

---

### Step 1：能力评估（必须先做）

- 当前系统已具备：
  - 用户画像能力
  - 推荐逻辑（规则 / scoring）
  - 缓存机制
  - 行为推断
  - 决策
  - 分析

- 存在问题：
  - 精度不足？
  - 实时性不足？
  - 可扩展性不足？
  - 架构层问题（耦合、扩展性、单点瓶颈）
  - 数据层问题（结构设计、冗余、扩展困难）
  - 算法/推荐问题（如评分模型不足、特征缺失）
  - 工程问题（性能、可维护性）

---

### Step 2：核心升级方向（重点）


输出 3-8 个核心升级点，例如：

- 用户画像冗余重复减法
- 推荐引擎现有架构优化,不增加新能力
- 决策系统增强
- 现实策略
- 食物推荐种类合理化，如大众化，不单原材料，贴近现实和场景
- 可维护性
- 完善所有策略种子数据

每个必须说明：

- 为什么需要
- 解决什么问题

---


### Step 3：架构升级设计（VNext）
- 给出优化后的架构图（文字说明即可）
- 标注新增模块（如特征工程层、策略层、缓存层等）

---

### Step 4：模块级升级设计（必须可落地）

按模块输出：

#### 1. Profile 模块（用户画像）
- 如何从“静态 + 定时” → “实时 + 推断”
- 是否引入事件驱动（event-based）

#### 2. Recommendation 模块
- scoring → strategy → hybrid AI
- 是否支持多策略组合

#### 3. Nutrition / Scoring
- 是否引入更精细指标（如 addedSugar vs naturalSugar）

#### 4. Cache / 性能
- 是否引入多级缓存
- 是否增加异步计算

#### 5. 数据流
- 是否引入事件流（user action → profile → recommendation）

---

### Step 5：技术路线图（必须分阶段）

#### Phase 1（短期）
- 可快速上线
- 优先“可执行性”和“真实场景”


#### Phase 2（中期）
- 策略引擎 + 场景系统 + 替代机制 + 可解释性
- 架构增强
- 新模块引入

#### Phase 3（长期）
- AI化 / 自动学习
- 个性化模型
- 国际化

---

### Step 6：如需数据迁移

- 完成数据迁移脚本和数据库初始化

---

### Step 7：文档升级（只输出差异）

输出：

- 新增章节
- 修改内容
- 删除内容（如有）

⚠️ 不要重写整个文档

🚫 禁止行为：

- 不要设计全新系统
- 不要输出不可实现的 AI 方案
- 不要空谈架构

---

🧠 工作模式：

你是在做：

👉 “从可用系统 → 可进化智能系统”的升级设计



生成V7.9的文档文件输出到/docs,然后开始直接迭代升级文档的Phase 1 到Phase 3
