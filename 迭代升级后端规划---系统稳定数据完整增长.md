你现在是一个后台系统优化专家 + 数据工程师 + API架构师。

当前系统已经上线运行，请注意：

你的任务不是优化整个系统，而是：
【只对以下两个模块进行增强优化】

---

# 🎯 允许修改范围（严格限制）

仅允许优化：

1. 食物管理模块（Food Management）
2. AI数据补全模块（AI Enrichment，在 food-pipeline 下）
3. 项目没上线，旧的冗余可以去掉


---

# 🚫 禁止修改范围（必须遵守）

以下内容一律禁止修改或扩展：

- 推荐系统
- 用户画像系统
- 决策系统
- 可解释性系统
- 订阅/商业化逻辑
- 其它后台模块
- 不要增加数据库字段

如果涉及这些模块：
👉 只允许“读取”，不允许“改动或优化”

---


# 🎯 已知问题

AI补全
1./enrichment/stats 统计面板异常
2.全库补全进度面板为空
食品库
http://127.0.0.1:3006/api/admin/food-library/statistics?_t=1776136379844
的统计数据不准
---

# 🎯 优化目标

仅在这两个模块内提升：
1. 可清除任务队列
2. http://127.0.0.1:3006/api/admin/food-pipeline/enrichment/stats?_t=1776136359775
汇总数据
http://127.0.0.1:3006/api/admin/food-library/statistics?_t=1776136379844
的统计数据
3. 数据完整性（食物库）
4. AI补全成功率与可控性
5. 后台可运营能力（批量 / 可视化 / 可回滚）
6. API能力完整性（支撑后台操作）
7. 补全字段的完善,尽可能补全foods相关的字段
8. 数据流转和状态更新
9. 旧的迭代冗余优化
10. AI补全提示词优化,获取更完整的数据
11. 汇总数据更新


---



# Step 1：现有能力分析（限定范围）

只分析：

- 食物管理现有功能
- AI补全现有流程
- 当前已有 API 能力

输出：
- 当前问题（只限这两个模块）
- API能力不足点（哪些操作做不了 / 不好做）

---

# Step 2：食物管理增强（重点）

在不新增新系统的前提下优化：

必须增强：

## （1）数据完整度管理
- 标记缺失字段
- 支持按“缺失程度”排序

---

## （2）数据质量控制
- 标记AI生成数据
- 支持人工修正
- 支持标记审核状态

---

## （3）数据可视化
- 显示字段完整情况
- 显示数据来源（AI / 手动）
- 显示置信度（如有）

---

⚠️ 限制：
- 不允许新增新的业务模块
- 只允许在现有 food 数据结构上做“轻量增强字段”（如 isReviewed）

---

# Step 3：AI补全模块增强（核心）

必须优化：

---

## （1）补全方式优化（关键）
- 从“一次补全全部字段” → 改为“按字段分阶段补全”
- 支持指定字段补全（如 nutrition / tags）

---

## （2）补全流程可控

后台必须支持：

- 手动触发补全
- 批量补全（按筛选条件）
- 查看补全执行状态（任务级别）

---

## （3）补全结果可视化（关键）

- 原始数据 vs AI补全数据对比
- 标记新增字段 / 修改字段
- 可查看字段级别补全结果

---

## （4）入库控制（重点）

- 不自动覆盖已有字段
- 支持人工审核后入库
- 支持选择性入库（字段级别）

---

## （5）失败处理机制

- 标记补全失败字段
- 支持失败重试（任务 / 单条 / 单字段）

---

# Step 4：流程优化（只限这两个模块）

## 场景1：批量补全流程

筛选缺失数据  
→ 创建补全任务  
→ 执行补全  
→ 查看结果对比  
→ 审核 需要有可信度等参考  
→ 入库  

---

## 场景2：人工修正流程

查看食物  
→ 编辑字段  
→ 标记为已审核  

---

# Step 5：API能力设计（关键输出）

请基于上述后台功能，自行设计【最小必要API集合】：

要求：

- 只设计“支撑这些功能所必须的 API”
- 不允许设计无关 API
- 优先复用现有 API 能力（如 CRUD）
- 仅在必要时新增 API

输出内容：

1. API能力分组（食物管理 / AI补全任务）
2. 每类操作需要哪些API能力（不用写死路径）
3. 哪些是已有 API 可以复用
4. 哪些必须新增（说明用途即可）

⚠️ 注意：
- 不需要输出具体 API path / method / DTO
- 重点是“能力设计”，不是接口文档

---

# Step 6：数据结构增强（允许范围内）

允许对 food 数据结构增加辅助字段：

- 数据来源标记（AI / 手动）
- 置信度（0~1）
- 审核状态

⚠️ 不允许改变核心字段结构

---

# Step 7：分阶段优化（必须）

Phase 1：
- 缺失字段识别
- 单条补全
- 基础操作可用

---

Phase 2：
- 批量补全
- 补全任务管理
- 结果对比

---

Phase 3：
- 数据质量控制
- 审核机制
- 补全策略优化

---

# Step 8：防跑偏机制（必须遵守）

如果你的方案涉及其它模块优化，请停止，并改为：

“该问题涉及系统其它模块，但本次优化范围不包含，暂不处理”

---

# 输出要求

- 所有优化必须局限在这两个模块
- 必须包含“API能力设计”（不是接口细节）
- 必须可直接用于开发落地
- 不要泛泛而谈



设计后更新V8.2文档/docs并迭代Phase1 到Phase 3的全部任务