你是一名资深数据架构师 + AI系统架构师，请帮我设计一个“全球化食物数据库系统”。

【目标】
构建一个可扩展、支持AI自动标注、支持多数据源融合的食物库（Food Database），用于减肥/营养分析App。

--------------------------------

【1. 数据结构优化】
我已有基础结构（包含：category、subCategory、nutrition等），请你：

1）优化数据结构，要求：
- 支持国际化（多语言、多地区）
- 支持多数据来源（USDA / OpenFoodFacts / 爬虫等）
- 支持AI标注字段（tags / confidence / source）
- 支持版本控制（数据更新）
- 支持冲突数据合并（多个来源不一致）

2）输出：
- JSON Schema（完整字段设计）
- 每个字段解释
- 是否必须字段（required/optional）

--------------------------------

【2. 数据来源方案设计】

请分析并设计食物数据获取方案，覆盖：

- 官方数据源（如 [USDA FoodData Central](chatgpt://generic-entity?number=0)）
- 开源数据库（如 [Open Food Facts](chatgpt://generic-entity?number=1)）
- 商业API（如 [Edamam](chatgpt://generic-entity?number=2)）
- 爬虫来源（如外卖平台、菜谱网站）

要求：
- 每个来源说明：可获取数据类型、优缺点、法律风险
- 是否可商用
- 是否建议长期依赖
- 是否需要人工清洗

输出：
- 数据来源对比表
- 推荐组合方案（主数据源 + 辅助）

--------------------------------

【3. 数据加工与标准化流程（重点）】

设计完整数据Pipeline：

输入 → 清洗 → 标准化 → AI标注 → 校验 → 入库

要求：
- 每一步提供具体实现方式（代码级 or 架构级）
- 标准化策略（单位统一 / 字段映射）
- 去重策略（同一食物不同来源）
- 冲突处理策略（如热量不一致）
- 标签体系生成逻辑

输出：
- Pipeline流程图（文字即可）
- 每一步的详细说明

--------------------------------

【4. AI辅助标注方案】

设计AI标注系统，包含：

1）使用云模型（如 [OpenAI](chatgpt://generic-entity?number=3) / [DeepSeek](chatgpt://generic-entity?number=4)）
2）使用本地模型（如 ComfyUI + 开源模型）

要求分析：
- 成本（按1万条数据估算）
- 精度
- 可扩展性
- 是否可商用

并输出：
- 推荐方案（云 or 本地 or 混合）
- Prompt设计策略
- JSON输出约束方案

--------------------------------

【5. 本地AI（ComfyUI）实施方案】

如果采用本地AI，请设计：

- 模型选择（食物识别 / 分类 / 分割）
- ComfyUI workflow设计
- 硬件要求（GPU）
- 部署架构（单机 / 集群）

--------------------------------

【6. 最终输出】

请输出：
- 一套完整可实施方案（技术 + 产品）
- MVP阶段方案（1周可落地）
- 中长期演进路线