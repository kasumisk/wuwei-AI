你现在是一个系统架构师 + NestJS专家 + 数据规范治理专家。

当前系统存在严重的字段不统一问题：

- 数据库：snake_case（下划线）
- 后端API：camelCase + 部分嵌套
- 前端：admin / web 各自处理字段（不统一）

---

# 🎯 你的任务

目标是整个工程下的3个项目admin, api-server, web接口统一数据标准 DTO (camelCase)改造：
DB（snake_case）
   ↓
Prisma（@map → camelCase）
   ↓
NestJS Entity / Service（camelCase）
   ↓
DTO（统一输出标准）
   ↓
前端（admin + web 直接使用）
   ↓
AI补全（必须输出 DTO 结构）

---

# 🚫 强约束（必须遵守）

- 不改变数据库原有字段命名（继续 snake_case）
- 不破坏现有业务逻辑

---

持续一直修改调整，直到admin, api-server, web三个项目符合数据统一标准