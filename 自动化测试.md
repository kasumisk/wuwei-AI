你现在是资深后端性能优化工程师 + SRE + 自动化测试工程师。

背景：
当前项目是 NestJS 后端，部署在 Cloud Run，数据库为 Postgres， Redis cloud，部分异步任务已迁移到 Cloud Tasks / Cloud Run Jobs。现在有一个接口响应不稳定，经常超时2500ms不能返回实时推荐，需要你针对该接口做完整自动化调试、压测、定位和优化。

我会提供：
- 接口 URL：https://api.eatcheck.app/api/app/food/meal-suggestion/adjust
- HTTP Method：POST
- 测试 token：eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJlOGM1MDEyZi05ZTVhLTQ5NjYtOWIzMS1iYWJlM2IwODM2ZjgiLCJhdXRoVHlwZSI6InBob25lIiwidHlwZSI6ImFwcCIsImlhdCI6MTc3Nzg5MDkxNywiZXhwIjoxNzc4NDk1NzE3fQ.zFprSsjWw5sr6a2Y-94FwYzJ_7YSoSyjYt5KURnGB6A
- 预期响应：日志里的响应log正常
- 性能目标：不会超时回退

任务目标：
不是只分析，而是要实际请求接口、复现问题、定位原因、修复代码，并反复测试，直到接口稳定达到预期。

严格要求：

一、接口自动化测试
1. 使用我提供的 token 实际请求接口。
2. 覆盖以下测试：
   - 单次请求
   - 连续 20 次请求
   - 并发 5 / 10 / 20 请求
   - 冷启动后首次请求
   - 相同 payload 重复请求
   - 不同 payload 请求
3. 记录每次请求：
   - status code
   - response time
   - response body 关键字段
   - error message
   - traceId / requestId
4. 统计：
   - avg latency
   - p50 / p90 / p95 / p99
   - error rate
   - timeout rate
   - 慢请求样本

二、服务端定位
请结合项目代码和日志排查：
1. Controller / Service / Repository 调用链
2. 数据库查询是否慢、是否 N+1
3. Prisma 查询是否缺索引、是否 include 过重
4. Redis cache 是否命中、是否异常降级
5. 外部 AI / 第三方 API 是否导致不稳定
6. Cloud Tasks / async task 是否影响接口同步响应
7. 是否存在冷启动、重复初始化、全局 provider 副作用
8. 是否存在同步计算过重
9. 是否存在超时配置不合理
10. 是否存在错误吞掉后返回不稳定结构

三、优化原则
1. 不允许只靠加 timeout 掩盖问题。
2. 不允许破坏现有 API 响应结构。
3. 不允许改动无关模块。
4. Redis 只能作为加速层，不能作为唯一状态来源。
5. 如果涉及缓存，需要设计：
   - cache key
   - TTL
   - invalidation
   - Redis 不可用降级策略
6. 如果涉及数据库，需要：
   - 给出慢查询
   - 给出索引建议
   - 添加 Prisma migration
   - 验证 explain / 查询耗时
7. 如果涉及外部 API，需要：
   - 增加超时
   - retry with backoff
   - fallback
   - 明确错误返回

四、修复后验证
每一轮修复后必须重新跑测试：
1. 单次请求
2. 连续 20 次
3. 并发 5 / 10 / 20
4. 至少重复 3 轮
5. 对比修复前后数据

必须输出：
- 修复前测试数据
- 问题根因
- 修改文件清单
- 修改说明
- 修复后测试数据
- 是否达到目标
- 剩余风险

五、性能目标
请按以下目标验收：
- 成功率：>= 99%
- p95：<= ____ ms
- p99：<= ____ ms
- error rate：<= 1%
- 响应结构稳定一致
- 并发 20 下不出现明显雪崩

六、自动化测试工具要求
优先使用项目已有测试工具；如果没有，请新增一个临时脚本，例如：
- scripts/debug-api-stability.ts
- scripts/load-test-endpoint.ts

脚本需要支持：
- URL
- method
- token
- body
- concurrency
- repeat
- timeout
- 输出 JSON 报告

七、安全要求
1. token 只能用于本次测试。
2. 不要把 token 写入仓库。
3. 不要提交 token 到日志。
4. 测试脚本通过环境变量读取 token。
5. 不要对生产写入破坏性数据；如接口会写数据，必须说明影响并优先使用测试账号。

执行顺序：
1. 先阅读接口相关代码。
2. 再写测试脚本。
3. 先跑 baseline 测试。
4. 根据数据定位问题。
5. 修复最小必要代码。
6. 重复测试。
7. 达标后输出最终报告。