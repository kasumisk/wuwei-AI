下面这份可以直接发给 AI / Cursor：

````md
你现在是一个高级后端架构师 + 订阅支付系统架构师 + NestJS/Flutter 全栈专家。

我正在开发一个 AI 饮食健康 App：EatCheck。项目尚未上线，可以接受较大范围重构。当前已经接入 RevenueCat，用于 Apple App Store 和 Google Play 订阅。未来还可能接入微信支付、支付宝、Stripe、PayPal 等其他支付/订阅渠道。

请你基于当前项目代码，全面检查并优化订阅系统架构。

项目结构：
- 后端：./server，NestJS
- Flutter App：请根据当前 Flutter 项目代码分析
- 当前订阅三方：RevenueCat
- 当前目标市场：海外优先，后续可能兼容中国大陆
- 当前状态：未上线，可以重构数据库、服务层、接口、前端订阅流程

## 核心目标

请不要只修 bug，而是从“上线前生产级订阅系统”的角度重新审视：

1. 当前订阅系统是否可靠
2. 数据模型是否合理
3. 是否能正确处理 Apple / Google / RevenueCat 回调
4. 是否支持未来多支付渠道扩展
5. 是否能支持用户匿名登录后升级账号
6. 是否能支持恢复购买、跨设备同步、订阅状态刷新
7. 是否能处理退款、取消、续订失败、过期、宽限期、账单重试等状态
8. 是否能保证后端权限判断可信
9. 是否能方便后续扩展微信 / 支付宝 / Stripe / PayPal
10. Flutter 前端订阅页、权益判断、错误处理是否完整

## 请重点检查

### 1. 数据库模型

请检查当前数据库中订阅相关表设计，判断是否需要重构，例如：

- users
- subscriptions
- purchases
- entitlements
- payment_events
- revenuecat_events
- plan/products
- user_entitlements
- subscription_history
- webhook_logs

请分析是否需要区分：

- 订阅计划 Plan
- 商店商品 Product
- 用户权益 Entitlement
- 订单 Purchase
- 订阅 Subscription
- 支付事件 PaymentEvent
- 第三方原始事件 ProviderEvent
- 用户当前权限 UserEntitlement

要求：
- 给出推荐的数据模型
- 给出字段设计
- 给出 Prisma schema 修改建议
- 说明每张表职责
- 说明哪些字段必须唯一索引
- 说明哪些字段需要幂等处理
- 说明如何兼容 RevenueCat / 微信 / Stripe

### 2. RevenueCat 接入检查

请检查当前 RevenueCat 接入是否存在问题：

- App User ID 设计是否合理
- 匿名用户与注册用户如何绑定
- restore purchases 是否完整
- webhook 是否已接入
- webhook 签名校验是否安全
- webhook 事件是否幂等
- 是否保存原始事件
- 是否用 RevenueCat 的 entitlement 作为最终依据
- 是否支持手动刷新用户订阅状态
- 是否处理 sandbox / production 环境
- 是否避免前端伪造订阅状态

请给出生产级实现方案。

### 3. 后端权限系统

请检查当前后端如何判断用户是否为 Premium。

要求设计一个统一的权限判断体系：

- 后端不能相信 Flutter 本地状态
- 后端应以数据库中的有效权益为准
- RevenueCat webhook 更新权益
- 客户端可主动 refresh subscription
- 接口层应有 Premium Guard / Subscription Guard
- 支持不同权益，例如：
  - unlimited_ai_analysis
  - advanced_nutrition_report
  - export_history
  - priority_model
  - family_plan
  - region_specific_features

请给出：
- Guard 设计
- Service 设计
- DTO 设计
- API 设计
- 缓存策略
- 失效策略

### 4. 多渠道支付架构

请不要把系统写死为 RevenueCat。

请设计一个可扩展架构：

```ts
interface SubscriptionProvider {
  getProviderName(): PaymentProvider;
  verifyReceipt(input): Promise<VerifiedPurchase>;
  syncUserEntitlements(userId): Promise<UserEntitlement[]>;
  handleWebhook(payload): Promise<ProviderEventResult>;
}
````

请根据项目实际情况设计更合适的接口。

未来需要支持：

* revenuecat
* wechat_pay
* alipay
* stripe
* paypal

要求：

* 目前仍以 RevenueCat 为主
* 但数据库和服务层不要强绑定 RevenueCat
* 原始 provider event 独立保存
* 统一转换成内部 subscription event
* 内部状态机统一维护权益

### 5. 订阅状态机

请设计一套清晰的状态机，至少覆盖：

* trial
* active
* grace_period
* billing_retry
* paused
* canceled
* expired
* refunded
* revoked
* transferred
* unknown

要求说明：

* RevenueCat 事件如何映射到内部状态
* Apple / Google 状态如何映射
* 每种状态是否拥有权益
* expireAt / renewAt / canceledAt / refundedAt 如何处理
* 重复事件 / 乱序事件如何处理

### 6. API 设计

请检查并优化订阅相关接口，例如：

```http
GET /api/app/subscription/status
POST /api/app/subscription/refresh
POST /api/app/subscription/restore
POST /api/webhooks/revenuecat
GET /api/app/subscription/plans
GET /api/app/entitlements/me
```

要求：

* 给出最终 API 设计
* 给出请求响应结构
* 说明哪些接口需要登录
* 哪些接口给 Flutter 用
* 哪些接口给 webhook 用
* 如何处理错误码
* 如何支持 微信等未来渠道

### 7. Flutter 前端订阅流程

请检查 Flutter 当前订阅流程：

* 初始化 RevenueCat SDK 是否正确
* appUserId 是否与后端 userId 对齐
* 匿名登录用户如何处理
* 注册/绑定账号后如何 RevenueCat logIn / logOut
* 订阅页 paywall 是否合理
* 购买中 loading 是否防重复点击
* purchase 成功后是否主动通知后端 refresh
* restore purchases 是否完整
* entitlement 缓存是否正确
* 离线状态如何展示
* 订阅失败、取消支付、网络错误如何处理
* TestFlight / Sandbox / Production 环境如何区分

请给出 Flutter 侧推荐架构，例如：

* SubscriptionRepository
* SubscriptionService
* SubscriptionProvider / Riverpod Provider
* PremiumState
* PaywallPage
* RestorePurchaseButton
* SubscriptionStatusBanner

### 8. 安全与合规

请检查：

* webhook secret 是否安全
* RevenueCat API Key 是否放错端
* 是否把 secret 暴露在 Flutter
* 是否有重放攻击风险
* 是否有伪造订阅风险
* 是否记录必要审计日志
* 是否符合 Apple / Google 订阅审核要求
* 是否提供 Terms / Privacy / Restore Purchases 入口
* 是否避免医疗承诺类权益描述

### 9. 测试方案

请给出完整测试清单：

* 单元测试
* 集成测试
* webhook 事件测试
* RevenueCat sandbox 测试
* TestFlight 测试
* Google Play internal test 测试
* 订阅成功
* 续订
* 取消
* 过期
* 退款
* 恢复购买
* 同一 Apple ID 多账号切换
* 匿名用户升级正式账号
* 乱序 webhook
* 重复 webhook
* 网络失败重试
* 后端权限实时刷新

### 10. 输出要求

请基于当前代码实际分析，不要只给通用方案。

请输出：

1. 当前订阅系统现状分析
2. 当前问题清单，按严重程度分级：

   * P0 上线前必须解决
   * P1 强烈建议解决
   * P2 后续优化
3. 推荐的新架构
4. 数据库模型设计
5. Prisma schema 修改方案
6. NestJS 模块结构设计
7. Provider 抽象设计
8. RevenueCat Webhook 生产级实现方案
9. Flutter 订阅流程重构方案
10. API 设计
11. 状态机设计
12. 权益系统设计
13. 迁移步骤
14. 测试清单
15. 最终上线前 checklist

## 约束

* 项目尚未上线，可以大幅重构
* 但不要为了抽象而过度复杂
* 目前 RevenueCat 是主渠道
* 后端必须作为订阅权限最终可信来源
* Flutter 不能作为 Premium 判断最终依据
* 所有 webhook 必须幂等
* 所有第三方事件必须保存原始 payload
* 所有权益变更必须可追踪
* 代码要适合生产环境
* 请给出可以直接落地的改造步骤

```
```
