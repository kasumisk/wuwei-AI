# =============================================================================
# .dockerignore — 控制发送到 Docker daemon 的构建上下文
# 越小的上下文 = 越快的构建 + 更少敏感泄漏风险
# =============================================================================

# Node / 构建产物（builder 阶段会重新生成）
**/node_modules
**/dist
**/build
**/coverage
**/.turbo
**/.next

# Git / IDE / OS
.git
.gitignore
.gitattributes
.vscode
.idea
.DS_Store
*.swp
*.log

# 环境变量文件 —— 永远不进镜像
.env
.env.*
**/.env
**/.env.*

# 测试 / 文档（运行时不需要）
**/*.test.ts
**/*.spec.ts
**/__tests__
**/test
**/tests
**/*.md
docs

# 不参与 api-server 镜像的其他 monorepo 子项
apps/web
apps/admin
apps/miniapp

# 不参与 api-server 的包
packages/types
packages/utils

# CI / 本地开发工具
.github
.husky
.cursor
docker-compose*.yml
Dockerfile.dev

# 临时 / 运行时数据
tmp
temp
*.tmp
*.tgz
