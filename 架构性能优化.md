
你现在是一个资深后端架构师 + 云原生架构师 + 数据库性能专家 + DevOps/SRE + NestJS/Prisma 专家。

我准备将项目部署到生产环境，目标用户主要在北美。当前技术栈大致如下：

- Backend：NestJS
- ORM：Prisma
- Database：Neon PostgreSQL
- Redis：Upstash Redis
- 部署：Google Cloud Run
- 订阅：RevenueCat
- 客户端：Flutter App
- 产品：AI 饮食健康建议 App EatCheck
- 当前项目未正式上线，可以接受较大范围重构，但必须保证可落地

请你基于当前代码仓库进行一次“生产上线前架构与性能审查”，不要只给泛泛建议，要结合现有代码、模块、数据库结构、接口调用链和部署方式，输出可执行方案。

---

## 1. 总体目标

请从以下维度深入分析：

1. 性能优化
2. 架构合理性
3. 数据库与 Prisma 使用方式
4. Redis 缓存、锁、限流、幂等设计
5. Cloud Run 部署与运行时配置
6. Neon PostgreSQL 连接池与迁移策略
7. RevenueCat 订阅系统可靠性
8. AI 分析流程的稳定性与耗时优化
9. 日志、监控、告警、错误追踪
10. 安全性与生产配置
11. 可维护性、模块边界、代码结构
12. 上线前 checklist

---

## 2. 必须先做现状分析

请先阅读并分析项目代码，重点查看：

- `server` 或后端主目录
- Prisma schema
- migration 文件
- config/env 相关文件
- subscription / revenuecat 模块
- auth 模块
- food / nutrition / analysis / recommendation / decision 相关模块
- Redis 使用位置
- AI 调用位置
- 日志系统
- 全局异常处理
- 接口鉴权与限流
- 部署配置文件，如 Dockerfile、cloudbuild、env 示例等

然后输出：

```text
当前系统现状总结：
- 当前架构是什么
- 当前核心模块有哪些
- 当前数据流是什么
- 当前最容易出问题的地方是什么
- 上线前最高优先级风险是什么
````

---

## 3. 性能分析要求

请重点检查：

### API 性能

* 是否存在接口过重
* 是否有 N+1 查询
* 是否有重复查询
* 是否有同步阻塞逻辑
* 是否有大模型调用阻塞主请求
* 是否应该拆成异步任务
* 是否存在过度日志输出
* 是否存在不必要的全量查询

请输出：

```text
接口性能风险列表：
- 问题位置
- 原因
- 影响
- 优先级
- 推荐修改方案
```

---

## 4. PostgreSQL / Prisma 优化要求

请重点分析：

* Prisma schema 是否合理
* 索引是否足够
* 外键关系是否合理
* JSON 字段是否滥用
* Decimal / Float / Int 使用是否合理
* migration 是否存在风险
* 是否需要补充唯一约束
* 是否有幂等约束
* 是否有软删除 / 审计字段
* 是否适合 Neon pooled connection
* `DATABASE_URL` / `DIRECT_URL` 是否应该分开
* Cloud Run 下 Prisma 连接数是否安全

请输出：

```text
数据库优化建议：
1. 必须修改
2. 强烈建议修改
3. 可以后续优化
```

并给出具体 Prisma schema 修改建议。

---

## 5. Redis / Upstash 设计要求

请检查 Redis 当前用途，并设计生产级方案：

* 缓存 key 命名规范
* TTL 策略
* rate limit
* webhook 幂等
* AI 分析缓存
* 用户每日状态缓存
* 分布式锁
* 防重复提交
* 是否有不能被 eviction 删除的数据
* 是否需要拆分 cache Redis 和 queue Redis

请输出：

```text
Redis 生产级 key 设计：
- key
- value 示例
- TTL
- 是否允许丢失
- 使用场景
```

---

## 6. Cloud Run 生产配置要求

请分析并建议：

* region 选择，目标北美用户
* CPU / Memory
* concurrency
* min instances
* max instances
* request timeout
* env 管理
* health check
* graceful shutdown
* cold start 优化
* Dockerfile 是否合理
* 是否需要启用 keep-alive
* 是否需要设置 NODE_ENV=production
* 是否需要关闭 debug 日志

请输出推荐配置：

```text
Cloud Run production config:
- region:
- cpu:
- memory:
- concurrency:
- min instances:
- max instances:
- timeout:
- env:
- reason:
```

---

## 7. AI 分析流程优化要求

请重点检查 AI 饮食分析流程：

* 图片 / 文本分析是否阻塞请求
* 是否应该拆成 job
* 是否需要 SSE / 轮询 / WebSocket
* 是否需要结果缓存
* 是否有超时控制
* 是否有 retry 策略
* 是否有 fallback
* 是否记录 AI 原始响应
* 是否有 token 成本控制
* 是否有 prompt versioning
* 是否有结构化输出校验
* 是否有安全降级逻辑

请输出：

```text
AI 分析生产化方案：
- 当前问题
- 推荐流程
- 接口设计
- 数据表调整
- 错误处理
- 超时与重试策略
```

---

## 8. RevenueCat 订阅系统审查

请检查：

* webhook 是否幂等
* event_id 是否唯一
* 用户订阅状态是否可重建
* app_user_id 映射是否稳定
* Apple / Google / 未来微信支付是否可扩展
* 是否过度依赖客户端结果
* 是否有订阅状态同步任务
* 是否有失败重放机制
* 是否有审计日志
* 是否有 entitlement 抽象

请输出：

```text
订阅系统生产风险：
- 风险
- 影响
- 修复方案
- 是否必须上线前完成
```

---

## 9. 安全与合规

请检查：

* JWT / Firebase Auth 校验
* 匿名用户升级账号逻辑
* CORS
* rate limit
* API 权限控制
* webhook signature 校验
* env 泄漏风险
* 日志是否打印敏感数据
* 用户健康信息是否有隐私风险
* 删除账号 / 删除数据能力
* Terms / Privacy URL 是否准备好

---

## 10. 日志、监控、告警

请设计上线前最小可用监控方案：

* 请求日志
* 错误日志
* 慢查询
* AI 调用耗时
* RevenueCat webhook 失败
* DB 连接异常
* Redis 异常
* 用户关键行为
* 订阅状态异常

请推荐：

```text
最低成本方案：
- 日志：
- 错误追踪：
- 指标：
- 告警：
```

---

## 11. 输出格式要求

请严格按下面格式输出：

```md
# 生产上线前架构审查报告

## 一、当前系统现状

## 二、最高优先级风险 Top 10

## 三、性能优化建议

## 四、数据库 / Prisma 优化建议

## 五、Redis / Upstash 优化建议

## 六、Cloud Run 部署建议

## 七、AI 分析流程生产化建议

## 八、RevenueCat 订阅系统建议

## 九、安全与合规建议

## 十、日志、监控、告警方案

## 十一、上线前必须完成 Checklist

## 十二、可延后优化项

## 十三、建议的实施顺序
```

---

## 12. 重要约束

* 不要只讲理论
* 不要泛泛建议
* 必须结合当前代码
* 能给代码示例就给代码示例
* 能给 Prisma schema 修改建议就直接写
* 能给 Cloud Run 参数就直接写
* 区分“上线前必须做”和“上线后可以优化”
* 项目未上线，可以接受大范围重构
* 目标是生产级、可维护、可扩展、成本可控

```
